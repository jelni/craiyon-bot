services:
  craiyon-bot:
    build: .
    environment:
      TDLIB_COMMIT_HASH: f06b0bac65278b03d26414c096080e7bfecfef52
      USER_AGENT:
      TELEGRAM_TOKEN:
      API_ID:
      API_HASH:
      DB_ENCRYPTION_KEY:
      COBALT_INSTANCES:
      OPENROUTER_API_KEY:
      FAL_API_KEY:
      STABLEHORDE_TOKEN:
      STABLEHORDE_CLIENT:
      MAKERSUITE_API_KEY:
      GROQ_API_KEY:
    networks:
      - cobalt-api
    volumes:
      - craiyon-bot:/data

networks:
  cobalt-api:
    external: true

volumes:
  craiyon-bot:
